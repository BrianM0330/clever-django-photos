Django>=6.0,<6.1
channels>=4.2,<5
daphne>=4.1,<5
dj-database-url>=3.0
whitenoise>=6.8

# Background tasks: Django 6.0 ships `django.tasks` natively with the
# `immediate` backend. Add `django-tasks-db` here when you want a real worker
# process backed by SQLite/Postgres.
