[tool.ruff]
line-length = 120
target-version = "py313"
extend-exclude = ["TEMP_RAILS_REFERENCE", ".venv", "*/migrations/*"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]

[tool.black]
line-length = 120
target-version = ["py313"]
